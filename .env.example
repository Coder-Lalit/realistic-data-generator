# Copy to .env and adjust. Only MONGODB_URI and REDIS_URL belong here by default.
# All other settings use built-in defaults (see README → Environment variables).

MONGODB_URI=mongodb://127.0.0.1:27017/data-generator
REDIS_URL=redis://127.0.0.1:6379
